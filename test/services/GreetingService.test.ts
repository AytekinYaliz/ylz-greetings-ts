import greetingsJobInstance from "../../src/services/GreetingService";

describe("GreetingService class", () => {
  it("it should emit correctly", () => {
    expect(1).toBe(1);
    // const payload = { test: "test" };

    // eventEmitterInstance.on(
    //   EventName.GreetingsJobRunEvent,
    //   (expectedPayload) => {
    //     expect(expectedPayload).toEqual(payload);
    //   }
    // );

    // eventEmitterInstance.publish(EventName.GreetingsJobRunEvent, payload);
  });
});
